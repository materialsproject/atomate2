# LAMMPS input generated from LammpsInputFile with pymatgen v2024.10.29

# Sample input script template for MD

# Initialization

# Stage 3
units metal
atom_style atomic
dimension 3
boundary p p p

# Atom definition

# md.restart
read_data system.data

# Force field settings (consult official document for detailed formats)

# Stage 7
pair_style tersoff
pair_coeff * * /atomate2/tests/test_data/lammps/Si.tersoff Si

# Create velocities
velocity all create 300 42 mom yes rot yes dist gaussian

# Stage 9
neigh_modify delay 5 every 1 check yes

# Ensemble constraints
fix 1 all nve
fix 2 all langevin 300 300 0.1 42
# 3 all nose-hoover 300 300 0.1 iso 0 0 0.1

# Output settings
thermo 100
dump d1 all custom 100 traj.dump id element x y z vx vy vz fx fy fz
dump_modify d1 sort id element Si

# Actions
timestep 0.001
run 1000
write_restart md.restart
