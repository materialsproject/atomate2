# LAMMPS input generated from LammpsInputFile with pymatgen v2024.10.29

# Initialization
units metal
atom_style atomic
dimension 3
boundary p p p
pair_style tersoff

# AtomDefinition
read_data system.data

# ForceField
include forcefield.lammps

# Velocities
velocity all create 300 42 mom yes rot yes dist gaussian
neigh_modify delay 5 every 1 check yes

# Ensemble
min_style cg
fix 1 all box/relax iso 0 vmax 0.001

# Outputs
thermo 100
dump d1 all custom 100 traj.dump id element x y z vx vy vz fx fy fz
dump_modify d1 sort id element Si

# Actions
write_restart minimized.restart
minimize 1e-06 1e-06 1000 100000
