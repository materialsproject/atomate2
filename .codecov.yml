comment:
  layout: "diff, files"
  behavior: default
  require_changes: false
  require_base: no
  require_head: yes
coverage:
  status:
    project: off
    patch:
      default:
        only_pulls: true
